import Login from "../Authentication/Login/Login";
type Props = {};
const Home = (props: Props) => {
  return (
    <div className="font-poppins ">
      <Login />
    </div>
  );
};

export default Home;
